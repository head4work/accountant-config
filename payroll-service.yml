server:
  port: 8084
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payroll-service
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
